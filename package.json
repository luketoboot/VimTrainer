{
  "name": "vimtrainer",
  "version": "0.1.0",
  "description": "A browser game that builds Vim muscle memory through reflex and puzzle gameplay.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
